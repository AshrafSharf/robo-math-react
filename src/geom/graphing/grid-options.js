export class GridOptions {
  constructor() {
    // Simple boolean flags for grid and axis visibility
    this.renderGrid = true;
    this.renderAxis = true;
    
    // Label formatting options
    this.usePiLabels = false;  // Default to numeric labels
  }
}