 So the full breakdown:
  marrow(T, "rm", "E", 50, "squared", 20, 5)
         │    │     │   │      │      │   └── offset (5px from edge)
         │    │     │   │      │      └────── curvature (20px bend)
         │    │     │   │      └───────────── text label
         │    │     │   └──────────────────── length (50px)
         │    │     └──────────────────────── direction (East)
         │    └────────────────────────────── anchor (right-middle)
         └─────────────────────────────────── TextItem variable